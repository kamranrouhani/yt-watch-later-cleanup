# Review: pre-merge

- **When:** 2026-09-23 14:03
- **Stage:** after step 4 of 2026-09-23-sapisidhash-signing.md, before the PR
- **Scope:** commits
- **Branch:** feature/0004-sapisidhash-signing
- **Base:** main @ 5673b43
- **Head:** e8d8e57
- **Commits reviewed:**
  - e8d8e57 port the sapisidhash header from upstream
  - 9401d70 add the plan for the signing port
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| known input produces the expected hash, computed independently in the test with Node crypto | yes | `test/auth.test.js` recomputes SHA-1 over `<ts> <sapisid> <origin>` with `crypto.createHash` and compares the whole header; `npm test` `# tests 20 # pass 20 # fail 0` |
| the fallback order is tested | yes | `pickSapisid` tests cover SAPISID over both secure variants, 3PAPISID over 1PAPISID, 1PAPISID alone |
| missing cookies raise NotSignedInError | yes | empty string, unrelated cookies, empty value, and empty sapisid into buildAuthHeader, all asserted |

Scope from the issue: `src/core/auth.js` with `buildAuthHeader`, the
fallback order, `NotSignedInError`. Cookie reading stays outside the signing
itself: `pickSapisid` takes a cookie string, the page world hands it
`document.cookie`. All present.

## Checks

- **Attribution:** none in `git log --format=%B main..HEAD`. Author
  `Kamran Rouhani <kamran.rouhani@outlook.com>`.
- **Dashes:** none in changed files.
- **Comments:** none in `src/core/auth.js` or the test.
- **Secrets:** the only `sapisid=` strings are fake cookie values in the
  test. No real cookie, token or key.
- **Task files separate:** 9401d70 touches only `tasks/`.
- **Behaviour identical to upstream:** hash input `${nowSeconds} ${sapisid} ${origin}`,
  the same ten character class of timestamp, the same error message, the
  same fallback order, the same `decodeURIComponent`. Deviations found and
  pinned by tests rather than carried silently: upstream's matcher returns
  the first cookie of a repeated name, and does not match a space before
  the equals sign.

## Deviations from the plan

- Steps 3 and 4 landed as one commit. The port inside the WLCore wrapper
  already exposes the final API (`pickSapisid`, `buildAuthHeader`,
  `NotSignedInError`), and the tests were written against that API from the
  start, so a separate restructure commit would have had an empty diff.
  The two-commit shape in CONTRIBUTING exists so a port can be checked
  against the original; that check is in this review instead.

## Findings

1. Upstream's `getCookie` takes the first cookie of a repeated name. Real
   `document.cookie` can repeat a name, but in practice the SAPISID cookies
   appear once. Pinned as-is; #5 keeps upstream behaviour.
2. Nothing blocks the merge.
