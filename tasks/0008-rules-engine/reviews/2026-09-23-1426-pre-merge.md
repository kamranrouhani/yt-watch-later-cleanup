# Review: pre-merge

- **When:** 2026-09-23 14:26
- **Stage:** after step 3 of 2026-09-23-rules-engine.md, before the PR
- **Scope:** commits
- **Branch:** feature/0008-rules-engine
- **Base:** main @ a4de2aa
- **Head:** b17a5e1
- **Commits reviewed:**
  - b17a5e1 log the rules engine work
  - 64a608f evaluate remove and protect rules over entries
  - e7fb40e add the plan for the rules engine
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| protect beats remove, for every condition type | yes | safety test 1: eight remove rules across every field, four protect rules, zero removals, all entries protected. `npm test` `# tests 69 # pass 69 # fail 0` |
| a rule with no conditions matches nothing; a rule set with no remove rules removes nothing | yes | safety test 2, both halves |
| a condition on a missing field is a non-match and counted | yes | safety test 3 (null durationSeconds) and 4 (topic/category, even with enrichment present) |
| an invalid regex is a validation error, not a crash | yes | validate matrix includes `'(unclosed'`; seen failing when the regex check was removed |
| "among oldest N" counts only entries passing the other conditions of the same rule | yes | the explicit test: positions 1 and 2 removed, 3 not (watched), 4 not (position), boundary pinned by a separate test |
| the README example expressed as a test, topic stubbed | yes | five entries, remove [2], protected [1], skipped 3, with the skipped count corrected after printing the real evaluation |

Scope from the issue: `evaluate` returning
`{ remove, protected, skippedMissingData }`, the full operator set, topic
and category in the model but always skipped, versioned schema,
`validate` with readable errors naming rule and condition indexes. All
present.

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** none in the shipped modules or tests.
- **Secrets:** none.
- **Task files separate:** e7fb40e and b17a5e1 touch only `tasks/`.
- **Safety label honoured:** the four safety acceptance boxes were written
  as tests before any implementation existed, per the sprint plan's rule
  for `safety` issues.

## Findings

1. The skipped count uses a Set of entries, so one entry missing data on
   several rules counts once. The architecture asks for "how many entries
   were skipped", which is entries, not conditions. Correct as built.
2. The explicit topic/category missing guard is redundant with the generic
   undefined-field path. Removing it did not change any test outcome, which
   is double enforcement, not a hole. Left in place because #18 will make
   enrichment real and the guard is where that change lands.
3. Nothing blocks the merge.
