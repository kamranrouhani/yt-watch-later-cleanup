# Review round 2: #7 scanner (PR #39)

- **Ran:** 2026-09-24 10:04 UTC
- **Stage:** round 2 review, before merge
- **Covers:** `00254c2..504daac` (merge base `00254c23fe0c8bca33d8bdfe6209ce08c2e31660`, head `504daac4da73139826b1d26371f839e56cc03884`), focus on the round 1 rework `8c12ac5..504daac`
- **Commits since round 1:** 504daac restore sort drift check, seen-token guard and page throttle in the scanner; a7bcc8a add the round 1 fixes plan and progress entry for the scanner; daf648f add the round 1 review for the scanner
- **Lens:** execution. Suites rerun in the worktree, each round 1 fix mutated out of a scratch copy of `src/core/scanner.js` and the scanner suite rerun, file restored and `git status` clean after.
- **Verdict:** changes requested

## What passes

- `npm run check`: all files parse. `npm test`: 191 tests, 191 pass, 0 fail. `npm run test:browser`: 8 ok. `node --test test/scanner.test.js`: 16 pass. CI `test` passes on 504daac.
- Finding 1 (sort drift) landed and bites: with the first-page check replaced by `if (false)`, test 3 "sort drift on the scanned first page refuses and returns no entries" goes red (15/1). The check matches upstream lines 568 to 574, runs before any entry is added, and `SortDriftError` extends `SortNotVerifiedError`, so no positions are ever returned on drift. Missing sort data (`null` state) is refused too, since `undefined !== 2`.
- Sort verification is still polled to confirmation: with the final throw replaced by `return null`, test 1 goes red (15/1).
- Finding 3 (throttle) landed: removing the sleep turns test 5 red (15/1). Default 50 matches upstream `scanPageThrottleMs`.
- Finding 4 (aborted fingerprint) landed: forcing a fingerprint on every status turns the abort test red (15/1). Decision recorded in `plans/2026-09-24-1000-scanner-round1-fixes.md`.
- Finding 5 landed: the progress entry at 09:59 records the skipped port-first rule and its cost, and 504daac cites upstream lines.
- Acceptance box 1 now serves a clone of the captured page 1 with the oldest-first item marked selected (`withOldestSortSelected`), because the real capture reports newest first. The fixture file is untouched and the reason is logged. Acceptable.
- Author and committer Kamran on every commit, no attribution, no em or en dashes in changed files, task files in their own commits.

## Findings

1. **The seen-token test hangs instead of failing when the guard is removed.** Round 1 asked for the bite to be bounded ("bound the fake so the red run fails instead of hanging"). With the `seenTokens.has(nextToken)` break replaced by `if (false)`, `node --test test/scanner.test.js` never finishes; under `--test-timeout=5000` the file is cancelled, not reported as a failing assertion. In CI that would be a stalled job, not a red test naming the regression. Required: bound the fake in "pagination skips an already-consumed continuation token" (for example, throw from `browseContinuation` once `calls.continuation.length` passes a small cap), and show the mutation going red in the progress log.

2. **PR #39 body is stale.** It still shows `npm test` at 187, never mentions `SortDriftError`, the first-page drift check, the seen-token guard, the throttle or the null fingerprint on abort, and still says box 1 runs "against the three real captured fixture pages" without saying page 1 is served with its sort flipped to oldest first. The card's done line asks for each acceptance box with its command and real output; the drift refusal is the safety core of this issue and belongs beside the "never verifies" box. Required: update the body with the current commands and output from 504daac or later.

## Bites expected on rework

- Seen-token guard removed: the scanner suite reports a failing test, within the normal run time.
