# Review round 3: #7 scanner (PR #39)

- **Ran:** 2026-09-24 10:10 UTC
- **Stage:** round 3 review (last allowed round), before merge
- **Covers:** `00254c2..d620e7f` (merge base `00254c23fe0c8bca33d8bdfe6209ce08c2e31660`, head `d620e7f739e36cfa9caf8433f6c5d3ef0efd8845`), focus on the round 2 rework `408b8e3..d620e7f`
- **Commits since round 2:** d620e7f bound the seen-token regression test so it fails instead of hanging; 408b8e3 add the round 2 review for the scanner
- **Lens:** contract. The original card body and the issue's acceptance boxes, reread and checked against the head. Every finding from rounds 1 and 2 was checked by execution. Mutations ran against the worktree file, which was restored after each run, and `git status` was clean at the end.
- **Verdict:** approved, one residual risk listed below

## Acceptance boxes against head d620e7f

Each command below was run by name filter on Node v22.22.3.

- Captured pages: every item once, in order, positions 1..N. `node --test --test-name-pattern="against a fake client serving the captured pages" test/scanner.test.js` gives `ok 1 - against a fake client serving the captured pages, every item comes back once, in order, positions 1..N`.
- A sort that never verifies raises `SortNotVerifiedError` and returns no entries. `--test-name-pattern="never verifies"` gives `ok 1 - safety: a sort that never verifies raises SortNotVerifiedError and returns no entries`.
- A duplicated item across pages appears once. `--test-name-pattern="duplicated item across pages"` gives `ok 1 - a duplicated item across pages appears once, and later positions shift down for it`.
- Aborting mid scan stops within one page. `--test-name-pattern="aborting mid scan"` gives `ok 1 - aborting mid scan stops within one page: no continuation fetch happens after abort`.
- The fingerprint changes on add, remove and move. `--test-name-pattern="fingerprint changes"` gives three `ok` lines: added, removed, moved.
- The plan records the result object: `scannedAt`, `pageCount`, `entryCount`, and `fingerprint` of the ordered `setVideoId` list, plus `status` and `sortState`. The code at `src/core/scanner.js` returns exactly those fields.

## Safety weighting (card review brief)

- Sort verification is polled to confirmation: the edit ack is checked first, then a fresh browse, 6 attempts, 350 ms apart. Replacing the final throw breaks test 1 (confirmed in round 2).
- Positions are refused otherwise: the first-page drift check runs before any entry is added. Replacing it with `if (false)` on d620e7f turns `not ok 3 - safety: sort drift on the scanned first page refuses and returns no entries` red (15 pass, 1 fail).
- Missing data never matches. A `null` or absent sort state fails `!== 2` and is refused. An entry whose `setVideoId` is null is skipped before dedupe and positioning, so it never takes a position or goes into the fingerprint. An aborted scan returns `fingerprint: null`.

## Round 2 findings

1. Seen-token test bounded: landed. With `if (seenTokens.has(nextToken))` replaced by `if (false)`, `node --test --test-timeout=5000 test/scanner.test.js` gives `not ok 4 - pagination skips an already-consumed continuation token instead of looping` with the error `browseContinuation called 6 times, exceeding the test cap of 5`. The run shows 15 pass, 1 fail and 0 cancelled, in about 256 ms. There is no hang. The progress log step 6 records the red run.
2. PR #39 body refreshed: landed in substance. It now describes `SortDriftError`, the seen-token guard, the 50 ms throttle, the null fingerprint on abort and the flipped page-1 sort menu. It shows 191 tests.

Round 1 findings 1, 3, 4 and 5 were confirmed in round 2 and remain intact at d620e7f. The drift mutation above still bites.

## Suites

- `npm run check`: all files parse.
- `npm test`: 191 tests, 191 pass, 0 fail.
- `npm run test:browser`: 8 `ok`.
- `node --test test/scanner.test.js`: 16 pass, 0 fail.
- CI `test` (required) passes on d620e7f.

## Hygiene

Author and committer on all 9 branch commits are `Kamran Rouhani <kamran.rouhani@outlook.com>`. No attribution trailers or markers appear in any message. The changed files contain no em or en dashes. Task files sit in their own commits. The comments in `src/core/scanner.js` cite upstream line ranges or state a constraint, and none of them is addressed to a person.

## Residual risk (not blocking, round cap reached)

- **The evidence in the PR body is imprecise.** The `-t "..."` commands it shows are not a Node test filter. On Node 22, `-t` does not select by name, and each of those commands runs all 16 tests. So the quoted single `ok` lines and their numbers do not match what the commands print: the body says `ok 3` for the captured pages test, but it is test 7, and says `ok 2` for a drift test titled "a page whose own sort state has drifted...", but that title does not exist (the real one is test 3, "sort drift on the scanned first page refuses and returns no entries"). The behaviour the body claims is true, and this review reproduced it by execution with `--test-name-pattern`. Only the shown commands and output are off. The corrected commands and output are in the Acceptance section above and are posted on PR #39. There was no fourth round to fix the body in.
