# Review: pre-merge

- **When:** 2026-09-22 23:13
- **Stage:** step 11 of 2026-09-22-scaffold-extension.md, after CI went green on PR #23, before merge
- **Scope:** commits
- **Branch:** feature/0001-scaffold-extension
- **Base:** main @ 7075347
- **Head:** ee04731
- **Commits reviewed:**
  - ee04731 document the test commands and how to load the extension
  - 4eceb1c run the syntax check and unit tests in ci
  - 145a04d load the extension in real chrome as a test
  - a325a0b add a syntax check for every js and json file
  - 60d2f4f add the manifest and a dashboard the toolbar icon opens
  - c68eebb set up node tests and the WLCore module wrapper
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| `npm test` passes locally and in CI | yes | local `# tests 9 # pass 9 # fail 0`; CI run 35796324136 job `test` pass, its log shows the same three lines |
| manifest parses | yes | the issue's `python3 -c "import json; ..."` exits 0; `npm run check` prints `all files parse` |
| loads unpacked with no errors | yes on the build host | `npm run test:browser` asserts no page or console errors; `developerPrivate.getExtensionInfo` shows `ENABLED`, no manifest, runtime or install warnings. Manual checklist step 1 for a real Chrome |
| toolbar icon opens the dashboard | handler yes, click manual | listener registered, handler opens one tab and refocuses on a second call. The click is manual checklist steps 2 to 4 |

Scope items from the issue: manifest with `storage`, `tabs` and the one host
permission (pinned by `manifest.test.js`), toolbar action to a dashboard tab,
placeholder heading, WLCore wrapper under `src/core/`, `npm test` limited to
`test/*.test.js`, a smoke test loading the module both ways, CI on push to
main and on PRs. All present. CI also runs `npm run check`, one step more
than the issue asks.

## Checks

- **Attribution:** `git log --format=%B main..HEAD` has no trailer, tool name
  or emoji. Every commit is `Kamran Rouhani <kamran.rouhani@outlook.com>`.
- **Dashes:** no U+2014 or U+2013 in any changed file.
- **Comments:** none in the shipped js, html or css.
- **Secrets:** none. No captures exist yet.
- **Task files separate:** no commit in the range touches `tasks/`.
- **Permissions and origins:** only `storage`, `tabs`,
  `https://www.youtube.com/*`. No network code at all yet.

## Deviations from the plan

- `background.js` names its handler `openDashboard` so the browser test can
  drive it. The plan only asserted `hasListeners()`, which proves a listener
  exists but not what it does. Behaviour is unchanged.
- Playwright resolved to 1.63.0 under `^1.56.0`. Irrelevant to the result
  since the binary is pinned.

## Findings

1. **`node_modules/` ships in the unpacked extension.** Loading the repo root
   after `npm install` pulls in about 18 MB of Playwright, visible as Size
   17.9 MB on the details page. It loads without warnings, so nothing is
   broken today, but a packaged build must leave it out. Filed as a follow up
   rather than widened into #1.
2. **#22 names the wrong task folder.** Its body says
   `tasks/0001-sprint-1/plans/`; the folder is `tasks/0022-sprint-1/`. Fixed
   when ticking #1 in #22.
3. **Progress log timestamps for steps 5 to 8 were estimated**, not taken
   from `date`, and run ahead of the clock. Recorded as a correction entry in
   the log rather than edited.

Nothing blocks the merge.
