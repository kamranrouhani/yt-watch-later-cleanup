# Review: pre-merge

- **When:** 2026-09-23 14:14
- **Stage:** after step 5 of 2026-09-23-net-guard.md, before the PR
- **Scope:** commits
- **Branch:** feature/0002-net-guard
- **Base:** main @ ee6cd1b
- **Head:** f8836ea
- **Commits reviewed:**
  - f8836ea log the net guard work
  - 7b3267e pin the manifest wiring with a guard test
  - d264c57 scan shipped files for stray network calls and origins
  - 85b431f add the network chokepoint with an origin allowlist
  - a09def2 add the plan for the network guard
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| `net.js` rejects `https://example.com` and allows both allowlisted origins, covered by tests | yes | `test/net.test.js`: reject test asserts a `TypeError` naming the origin and a zero length call log; allow test sends both origins through to a stubbed fetch. `npm test` `# tests 41 # pass 41 # fail 0` |
| adding a stray `fetch(` to any shipped file makes `npm test` fail (show it, then revert) | yes | plant in `dashboard/dashboard.js` gave `not ok 27` with `dashboard/dashboard.js: fetch(`, reverted, clean |
| adding a stray origin makes `npm test` fail (show it, then revert) | yes | `https://evil.example.com/x` in a string gave `not ok 27` with `dashboard/dashboard.js: origin https://evil.example.com`, reverted, clean |
| `reference/` and `test/` are excluded from the scan | yes | scratch root with the same violations planted under `reference/`, `tools/`, `docs/`, `scripts/`, `node_modules/`, `tasks/`, `.github/` scanned to `[]`; unit test pins it |

Scope from the issue: `src/core/net.js` with an allowlist and rejection
before sending; `test/no-network.test.js` covering `fetch(`,
`XMLHttpRequest`, `sendBeacon`, `WebSocket`, `EventSource`, `import(`,
`new Worker(` outside `net.js` and any other `https://` origin;
`test/wiring.test.js` for manifest referenced files and duplicate lists.
All present.

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** none in the shipped module or tests.
- **Secrets:** none.
- **Task files separate:** a09def2 and f8836ea touch only `tasks/`.
- **`tools/` exclusion:** the scan skips it; `tools/capture.js` calls fetch
  by design and is not shipped. The manifest does not reference it.

## The vacuous test incident

The first version of the live scan test passed because the helper resolved
the repo root one directory short, so the shipped file list was empty and
the assertion compared `[]` to `[]`. It was caught by planting a violation
in `dashboard.js` and watching the suite stay green, fixed before commit,
and the incident plus the two attempts are recorded in the progress log.
The committed test is the one that goes red on the plants.

## Findings

1. `manifest.json` permissions are pinned again in `test/wiring.test.js`
   ("only storage and tabs"), duplicating `manifest.test.js`. Kept: the
   wiring test fails on duplicates and the pin belongs with the wiring
   checks, while `manifest.test.js` owns the permission policy.
2. Nothing blocks the merge.
