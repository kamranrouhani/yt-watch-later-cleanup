# Round 1 review: keep dev files out of the loaded extension

- **Date:** 2026-09-24 06:40 UTC
- **Stage:** pre-merge, PR #36, round 1 (first review round)
- **Scope:** commits 7a86814..8568414 on feature/0024-shipped-build
- **Merge base:** d3aa3be2dbdec5dcd55e705aa10ec883486dd3fb59
- **HEAD:** 85684142017ab0e4ee3f6a7b766783486dd3fb59 (equals the PR head)
- **Commits:**
  - 7a86814 add the plan for keeping dev files out of the loaded extension
  - fc9ed6d derive the shipped file list from the manifest, cover content scripts
  - 6a1334a add a build script that ships only manifest-listed files
  - 61d07f2 log steps 2 through 6 in the shipped build progress
  - 8568414 add the pre-merge review for the shipped build

## Lens

Round 1, artifact-first: the diff was read cold before the handoff
narrative, then every handoff claim was re-executed independently.

## Acceptance boxes

- a documented way to produce a load-unpacked build that contains only
  shipped files: met. `npm run build` writes `dist/` from
  `scripts/build.js` via `scripts/shipped-files.js`, derived from
  manifest.json plus the explicit set. README "Try it" and CONTRIBUTING
  "Loading the extension" point at `dist/`.
- a test fails if the build contains `node_modules/`, `test/`, `tasks/`
  or `reference/`: met by `test/build.test.js`, red-first evidence in the
  progress log (temporarily added `test` to EXTRA_SHIPPED_DIRS, assertion
  went red, restored).

## Independent verification run by the reviewer

- `npm run build` -> `built 17 files into dist`; `find dist -type f`
  lists exactly manifest.json, background.js, content/{bridge,page}.js,
  dashboard/*, src/core/*; `du -sh dist` -> 96K. Matches the PR body list
  exactly. No `dist` entry in git status (dist/ gitignored, line 3).
- Fresh Playwright load of `dist/` (repo root untouched, mirroring the
  harness's chromium-1223 executablePath): `ok dist/ loaded in Chrome,
  extension id iaohagjnggkhbalmaeikehpnpdmokfen, toolbar listener true,
  dashboard rendered, errors: []`. Reproduces the implementer's run,
  same extension id.
- `npm run check` -> all files parse.
- `npm test` 5 runs -> 151 pass, 0 fail each. One earlier run of the
  reviewer's own showed a single failure whose name was lost to tail
  filtering and did not reproduce in 5 subsequent runs; the progress log
  independently records the same one-run flake as the known
  test/remover.test.js:175 timing flake already logged against #33 and
  left as residual risk on PR #34. Not attributed to this change, no
  action for this card.
- `npm run test:browser` -> 8 ok, all lines matching the PR body,
  including the close and ensureTab-again cases.
- Red-first spot check of the widened scan: the stray content-script
  fetch fixture run against the pre-branch scan (d3aa3be
  no-network-scan.js) reports no violation; on HEAD the new test asserts
  it is caught. The scan widening is real, not a no-op.
- Drift check the card asked for: manifestPaths in
  scripts/shipped-files.js and the pre-existing copy in
  test/wiring.test.js produce identical arrays on the current manifest
  (both list background.js, content/{bridge,page}.js and the four
  manifest-referenced src/core files). wiring.test.js keeping its own
  copy is pre-existing on main and was called out in the plan as a
  deliberate mirror, not a regression.

## Repo rules

- Authorship: every commit is Kamran Rouhani
  <kamran.rouhani@outlook.com>, no trailers, no AI markers.
- No em or en dashes in any changed file (grep over the diff paths).
- No comments addressed to a person; the new files carry no comments.
- Task files in their own commits: 7a86814, 61d07f2 and 8568414 touch
  only tasks/, fc9ed6d and 6a1334a only code and docs.
- No secrets in the diff; no captured data touched.
- Loop discipline: the plan was committed alone before the first code
  commit, the progress log has one entry per plan step including the
  failed first approach (background.js initially omitted from
  EXTRA_SHIPPED_DIRS), matching the orchestrator's mid-run correction.
- Both mid-run orchestrator findings landed: shared shipped-file module
  in scripts/ (no test-helper dependency), scan widening made
  deliberate with its own red-first test and a PR body paragraph, zip
  dropped with the rationale documented.
- CI on the PR head: required `test` check pass (39s), run 35964988662.

## Findings

None blocking.

Minor notes, not defects:

- the second case in test/build.test.js exercises the test's own walk
  helper rather than production code; the production path is covered by
  the first case plus the red-first evidence, so this is a style note
  only.
- the implementer's pre-merge review file says "16 files" where the
  build and PR body say 17; the PR body is correct, the review file's
  count is off by one. Cosmetic, not worth a round.

## Verdict

Approved. Both acceptance boxes are met with reproducible evidence, the
repo rules hold, and every claim in the handoff reproduced exactly.

## Residual risk

- test/remover.test.js:175 timing flake, pre-existing, already logged
  against #33, needs its own issue (not filed).
- wiring.test.js still carries its own manifestPaths copy; it agrees
  with scripts/shipped-files.js today, but a future manifest field
  added to one and not the other would drift silently. Candidate for a
  later cleanup issue, not this card.
