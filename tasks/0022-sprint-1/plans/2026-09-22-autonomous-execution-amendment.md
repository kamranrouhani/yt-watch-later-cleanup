# Sprint 1 amendment: autonomous execution

- **Written:** 2026-09-22 23:05
- **Amends:** `2026-09-22-sprint-1.md`. Everything in it stands except the
  two sections named below.

## Why

Kamran is handing sprint 1 to a fresh session to execute. He approved git
and GitHub writes for the whole sprint, and asked that the executing session
tell him whenever a real artifact would help rather than only at the two
points the original plan named.

## Replaces "Where Kamran is needed"

Kamran is needed for #3 (the capture) and #17 (the live run), as before.
Beyond those, the executing session asks him for anything that would settle
a question better than a guess: a saved page, a HAR, screenshots, a Takeout
export, a UI opinion. Each request goes into
`implementation/BLOCKED.md`, and work continues on other unblocked issues
while it waits.

## Replaces step 7 of "Per issue loop"

Git writes are pre-approved for this sprint, on `origin` only: branches,
commits, pushes, PRs, regular merge commits, issue housekeeping. Squash and
rebase merges, force-pushing `main`, other remotes and identity changes stay
forbidden. The full statement is in `GOAL.md`.

## Added

`GOAL.md`, `LOOP.md`, `KICKOFF.md`, `scripts/status.sh`,
`implementation/STATE.md`, `implementation/BLOCKED.md` and
`reviews/README.md` in this task folder, and `HANDOVER.md` at the repo root.
#1 has its own plan in `tasks/0001-scaffold-extension/`, written ahead of the
per-issue loop because its code was proven on this host during the handover.
