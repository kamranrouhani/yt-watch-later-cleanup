# Review: pre-merge

- **When:** 2026-09-23 14:20
- **Stage:** after step 3 of 2026-09-23-innertube-client.md, before the PR
- **Scope:** commits
- **Branch:** feature/0005-innertube-client
- **Base:** main @ f1df0e7
- **Head:** d1a71da
- **Commits reviewed:**
  - d1a71da log the innertube port
  - 0636ed1 port the innertube client onto the network chokepoint
  - 4321fde add the plan for the innertube client
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| with a fake `fetch`, each call sends the same URL, headers and body upstream sends, asserted field by field | yes | three field-by-field tests over url, method, all six headers, and parsed body, plus params/visitorData presence rules. `npm test` `# tests 51 # pass 51 # fail 0` |
| each error type is raised by the matching fake response | yes | the matrix: 429 -> RateLimitedError, 401 -> AuthError, 403 -> AuthError, 500 -> HttpError, non-JSON 200 -> HttpError, `status: FAILED_TRANSACTION` -> EditRejectedError. The edit-status cases are driven through `editPlaylist` |
| `npm test` green, including the no-network guard | yes | the guard is green with the client in `src/`, and a planted bare `fetch(` in `dashboard.js` still fails it after the pattern change |

Scope from the issue: `browseWatchLater`, `browseContinuation`,
`editPlaylist`, config passed in, `WL_PARAMS` kept with its provenance
comment (the one comment the issue itself asks for), the four error types.
All present.

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** one, the `WL_PARAMS` provenance line the issue requires.
- **Secrets:** fake key `AIzaFake`, fake header. Nothing real.
- **Task files separate:** 4321fde and d1a71da touch only `tasks/`.
- **No window/document/location** in the module, pinned by a test.

## The guard incident

The first implementation destructured `fetch` out of deps and was caught by
the #2 guard on its first run. The guard was right, the module was wrong.
The client now calls `net.fetch` on an injected net instance. The scan
pattern gained a lookbehind so only bare `fetch(` calls outside `net.js`
fail, and the #2 acceptance plant was replanted and seen red again.

## Findings

1. The error-matrix test initially drove the edit-status case through
   `browseWatchLater`, which never checks edit status, so that case passed
   vacuously. Caught when the case refused to fail, fixed before commit.
2. `HttpError` parses the body before deciding, so a 429 with a JSON body
   is still a `RateLimitedError`, never a parse crash. Pinned by the
   non-JSON test.
3. Nothing blocks the merge.
