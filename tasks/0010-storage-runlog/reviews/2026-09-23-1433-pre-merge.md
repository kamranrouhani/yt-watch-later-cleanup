# Review: pre-merge

- **When:** 2026-09-23 14:33
- **Stage:** after step 4 of 2026-09-23-storage-runlog.md, before the PR
- **Scope:** commits
- **Branch:** feature/0010-storage-runlog
- **Base:** main @ 1ce15ea
- **Head:** 8d3831e
- **Commits reviewed:**
  - 8d3831e log the storage work
  - fba9cae persist the scan and run log without ever aborting a run
  - ca4d5c5 add the plan for the storage wrapper
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| a run log survives a simulated dashboard reload | yes | fresh `createRunLog` over the same fake area reads back status, both batches and entry snapshots. `npm test` `# tests 87 # pass 87 # fail 0` |
| a quota failure evicts the oldest log and the new write succeeds | yes | presets write over a 280 byte quota with two logs stored: oldest evicted, write lands, newest log and the presets both readable. Quota sized from measured envelope sizes so the first write genuinely fails |
| a write that still fails returns a warning and does not throw | yes | quota 0 write resolves `{ ok: false, warning }`; a non-quota `disk on fire` failure also resolves a warning. Both bites seen red |
| the exported JSON contains every removed videoId, title, channel and matched rule | yes | two runs, four removals across three batches, every field asserted. Bite seen red when matchedRule was dropped from the export |

Scope from the issue: typed get and set, versioned keys, warnings not
throws, runLog start/appendBatch/finish/abort/export, quota eviction with
one retry, a fake storage with a settable quota. All present.

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** none in the shipped modules or tests.
- **Secrets:** none.
- **Task files separate:** ca4d5c5 and 8d3831e touch only `tasks/`.
- **Note:** the commit title on fba9cae says "persist the scan and run log"
  while the subject line in the shipped history reads "persist the scan,
  presets and run log without ever aborting a run". Verified the latter is
  what is in git.

## Findings

1. Three planned bites initially did not bite, all in the tests rather
   than the code: no test covered non-quota failures, the eviction test
   could not distinguish which log was evicted, and no test wrote a
   non-log key over quota. All three gaps were closed with real tests and
   the bites then went red. Recorded in the progress log.
2. Eviction triggers on any quota failure, not only run log writes,
   following the issue text ("when a write fails for quota, evict the
   oldest run logs, then retry once") over my first reading.
3. The run id uses `Date.now()` plus randomness. It is not a security
   boundary, only a list key, and #15 can switch it to a counter if the
   log ever needs stable ordering beyond newest-first.
4. Nothing blocks the merge.
