# Pre-merge review: #35 remover sleep flake

- **When:** 2026-09-24 08:13
- **Stage:** self review before the PR, implementation run
- **Branch:** `fix/0035-remover-sleep-flake`
- **Merge base with main:** `04c3562`
- **HEAD:** `6680647`
- **Commits in range:**
  - `6680647` make the default sleep test read the timer instead of the clock
  - `748e093` add the plan for the remover sleep flake
- **Covers:** the full diff `04c3562..6680647`, code and tasks.

## What the change is

One test in `test/remover.test.js` rewritten. The default-sleep test no
longer measures wall clock across a 5 ms real pause; it swaps
`globalThis.setTimeout` for a recording wrapper for the duration of one
`run()`, asserts the schedule is exactly `[5]` (the pauseMs between the two
batches), and restores the timer in `finally`. `src/core/remover.js` is
untouched.

## Acceptance boxes

1. "the test proves the default sleep ran without depending on wall-clock
   precision, or allows for the known early wake-up": met, first branch.
   The delay is read off the `setTimeout` call itself, which is exact by
   construction. No `Date.now()` remains in the test. Bites: with
   `defaultSleep` as `Promise.resolve()` the test goes red on the schedule
   assertion; with `await sleep(pauseMs)` removed from the batch loop it
   goes red too (and takes the between-batches test with it). The test
   cannot pass without the default sleep.
2. "50 runs in a row": met. Loop output: `runs: 50, failures: 0`.

## Checks

- Full suite in the worktree: `npm run check` all files parse; `npm test`
  175 pass, 0 fail; `npm run test:browser` 8 ok.
- Flake reproduced before the change: 2 failures in 40 runs, both at
  line 175, both `Date.now() - start >= 5` falsy.
- `git log --format='%an <%ae>%n%B' 04c3562..HEAD`: both commits Kamran
  Rouhani <kamran.rouhani@outlook.com>, subjects plain and lowercase, no
  trailers, no attribution, no emoji, no AI markers.
- Dashes: `grep -rnP '\x{2014}|\x{2013}'` over the two changed files: no
  matches.
- Task files separate from code: 748e093 is tasks only, 6680647 is the
  test only.
- Secrets: none, the diff touches one test function.
- Comments addressed to a person: none in the diff. No comments added at
  all; the test body reads on its own.
- The remover's safety behaviour untouched: `src/core/remover.js` has zero
  diff against main (`git diff 04c3562..HEAD -- src/core/remover.js`
  empty).
- Scope: no other files touched. The other 13 remover tests unchanged.

## Residual

- The stub replaces `setTimeout` on `globalThis` for one awaited call.
  Node runs tests concurrently per file by default only across files, not
  within one, so nothing else in this file can observe the stub; and it is
  restored in `finally` on pass and fail alike.
- `npm run test:browser` was green but is not affected by this change; it
  was run as part of the standing pre-merge suite, not as evidence for the
  acceptance boxes.
