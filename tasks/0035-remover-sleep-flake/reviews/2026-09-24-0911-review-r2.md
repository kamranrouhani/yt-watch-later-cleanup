# Review round 2: #35 remover sleep flake

- **When:** 2026-09-24 09:11
- **Stage:** review lane, round 2 (execution lens), before merge
- **Branch:** `fix/0035-remover-sleep-flake`
- **Merge base with main:** `04c3562`
- **HEAD:** `f5a007c`
- **Commits in range:**
  - `f5a007c` log the rework steps and the second pre-merge review
  - `ac7c5f4` pin the awaited pause in the default sleep test
  - `fb9db29` add the round 1 rework plan for the sleep flake
  - `dfd37c6` add the round 1 review for the remover sleep flake
  - `382e426` log the sleep flake steps and add the pre-merge review
  - `6680647` make the default sleep test read the timer instead of the clock
  - `748e093` add the plan for the remover sleep flake
- **Covers:** the round 1 finding and its rework, re-tested by execution;
  the rest of the diff was verified green in round 1 and is unchanged
  except the rework test hunk and the two task-file commits.

## The round 1 finding, re-tested by execution

Finding 1 was: the timer-stub rewrite pinned the pause being scheduled
but not awaited, and the unawaited mutation `sleep(pauseMs)` left the
whole suite green, which the card's hard rule forbids.

The rework adds an await pin with no wall clock: the stubbed timer sets
`sleepResolved` inside its callback, and the recording innertube checks,
synchronously at entry to the second `editPlaylist` call, that the flag
was up (test/remover.test.js:180-195). The mechanism is sound by
microtask ordering alone: in the green path `run()` resumes from the
sleep's resolve in the same timer callback that sets the flag, and under
the unawaited mutation batch 2 enters in the same microtask turn as
batch 1, before any timer callback can run.

All three bites re-applied by me to `src/core/remover.js`, each run red,
each restored byte-identical (`git status --short` and
`git diff --stat` empty after each):

| Bite | Mutation | My result |
|---|---|---|
| 1 | `defaultSleep` returns `Promise.resolve()` | test 12 red, `# pass 13` `# fail 1` |
| 2 | `await sleep(pauseMs)` removed entirely | tests 11 and 12 red, `# fail 2` |
| 3 | `sleep(pauseMs)` without the `await` (round 1 finding) | test 12 red, `# pass 13` `# fail 1` |

Bite 3 is the mutation that was green before the rework and is red now,
which is the round 1 finding closed. After the last restore the suite
was back to `# pass 14` `# fail 0`.

## Everything else, re-run by me

- 50-run loop, counted:
  `fails=0; for i in $(seq 1 50); do node --test test/remover.test.js >/dev/null 2>&1 || fails=$((fails+1)); done`
  -> `runs: 50, failures: 0`.
- `npm run check`: all files parse. `npm test`: `# tests 175`
  `# pass 175` `# fail 0`. `npm run test:browser`: 8 ok lines.
- Scope: `git diff 04c3562..HEAD -- src/core/remover.js` empty, the
  remover's safety behaviour is untouched. The other 13 remover tests
  are byte-identical to round 1; the only code change since is the
  11-insertion hunk in the one test (ac7c5f4).
- CI: `test` check pass on head f5a007c (run 35979267790), PR
  MERGEABLE/CLEAN, open, not draft, body says `Closes #35` and shows
  both acceptance boxes with commands and real output.
- Authorship: all seven commits Kamran Rouhani
  <kamran.rouhani@outlook.com>, no trailers, no attribution markers
  (grep over the full bodies, no matches), plain lowercase subjects.
- Dashes: no em/en dashes in the test file or the task folder.
- Task files in their own commits: fb9db29 (plan only), ac7c5f4 (test
  only), f5a007c (progress log and self review only).
- New test shown failing first: satisfied by the bite table, the flake
  itself was the original failure and the three bites are the prove-it-
  bites step the card demands.

## Acceptance boxes

1. "the test proves the default sleep ran without depending on
   wall-clock precision": met. Schedule pinned exactly (`[5]`), await
   pinned by the flag observation, no `Date.now()` anywhere in the test.
2. "`node --test test/remover.test.js` passes 50 runs in a row": met,
   reproduced independently with a counted loop, `runs: 50,
   failures: 0`.

## Verdict

Approve. The single round 1 finding is fixed and verified by execution;
no new findings. Residual risk, not blocking: the await pin relies on
microtask ordering between a timer callback and a same-turn promise
chain, which is stable in Node's event loop but is the one part of the
test a future runtime change could theoretically disturb; the flag is
set before `fn()` runs in the stub, so the ordering is enforced by the
test's own stub, not just by the runtime.
