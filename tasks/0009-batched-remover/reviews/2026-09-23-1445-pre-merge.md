# Review: pre-merge

- **When:** 2026-09-23 14:45
- **Stage:** after step 3 of 2026-09-23-batched-remover.md, before the PR
- **Scope:** commits
- **Branch:** feature/0009-batched-remover
- **Base:** main @ 8fc25b5
- **Head:** 9beb5fb
- **Commits reviewed:**
  - 9beb5fb log the remover work
  - 50fa0ff remove exactly a frozen plan in small batches
  - e534adc add the plan for the batched remover
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| passing anything other than a plan from `createPlan` throws | yes | null, undefined, unfrozen lookalike, wrong kind, wrong version, array; all reject with `TypeError` and zero requests sent. `npm test` `# tests 101 # pass 101 # fail 0` |
| a 60 item plan with batch size 25 sends 3 requests of 25, 25, 10, exact `ACTION_REMOVE_VIDEO` payloads | yes | the fake innertube records every call; sizes asserted `[25, 25, 10]`, first, 25th and 60th actions asserted exactly |
| a 429 on batch 2 stops the run, batch 1 done, 2 and 3 not attempted | yes | `removedSetVideoIds` 25, one `onBatch`, two requests, `stopReason` matches /rate limited/ |
| abort between batches stops before the next request | yes | signal flipped in `onBatch` after batch 1: one request, 10 removed, `stopped`. Also abort-before-first sends nothing |
| a batch size of 0, 51 or a string is rejected | yes | plus -1 and 1.5, all `RangeError` before any request |

Scope from the issue: `run(approvedPlan, { batchSize, pauseMs, signal,
onBatch })`, plan from `createPlan(previewResult, scanFingerprint)` and
frozen, defaults 25 and 1500 ms, hard limits 1 to 50, stops on the three
named errors plus anything else, `onBatch` before the next batch. All
present. One deviation, logged: an `HttpError` also stops the run rather
than being swallowed, per the architecture's "any non-success status stops
the run".

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** none in the shipped module or tests.
- **Secrets:** none.
- **Task files separate:** e534adc and 9beb5fb touch only `tasks/`.
- **Safety label honoured:** the plan-gate, freezing and limit tests were
  written before any implementation.

## Findings

1. The batch-limit bite escalated into a deadlock: with the guard removed,
   `batchSize: 0` loops forever slicing empty batches. This is the guard
   doing its job in the most literal way. Full diagnosis chain (file
   bisect, single test, probe, file read) in the progress log.
2. `createPlan` copies entries rather than referencing the preview's, so a
   later mutation of the preview result cannot widen or alter the frozen
   plan.
3. Nothing blocks the merge.
