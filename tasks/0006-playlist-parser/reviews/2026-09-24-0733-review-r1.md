# Round 1 review, 2026-09-24 07:33 (UTC)

- **Stage:** round 1 independent review, artifact lens, before merge.
- **Branch:** `feature/0006-playlist-parser`
- **Merge base with main:** `b741d69e800991523c5458406d5cb954ab991dfd`
- **Head:** `2ee905611d6c81366a3c0bc85df05222c4a82c9f`
- **PR:** https://github.com/kamranrouhani/yt-watch-later-cleanup/pull/37
- **Commits in range:**

```
2ee9056 log steps 2 to 5 in the playlist parser progress
f14e9d4 add the pre-merge review for the playlist parser
6210b8e restructure the parser to parsePage and the Entry shape
69aaa6f port extractEntriesAndContinuation from upstream verbatim
5729959 add the plan for the playlist parser
```

- **Reviewer:** metis-review, kanban run 12 on t_ef9412e8.

## What was verified, independently

**Fields against the raw fixture JSON.** Every field-assertion in
`test/playlist-parser.test.js` was re-derived by walking the fixture
renderers directly, not by trusting the tests:

- `vid00000005` (watched): `lengthText` 8:44, resume overlay 100,
  byline run Channel 5 with browseId UCchannel000000000000005. Matches
  the asserted 524 / 100 / Channel 5 / UCchannel000000000000005.
- `vid00000007` (partial): 25:39, resume 33. Matches 1539 / 33.
- `vid00000001` (unwatched): 5:23, no resume overlay. Matches 323 / 0.
- `vid00000230`: `lengthText` 1:03:02. Matches 3782.
- `vid00000103` (page 2): byline run carries no `browseEndpoint`; the
  only browseIds in that renderer sit inside a collaborators dialog
  nested in `showDialogCommand`, which the parser correctly does not
  read. channelId is null, and the safety test proves a channelId
  remove rule cannot match it.
- `vid00000298` (page 3): no `shortBylineText` at all. channelName and
  channelId both null.
- Synthetic PlantVid002: `unplayableText` [Private video], no
  lengthText. Matches playable false, unavailableReason
  [Private video], durationSeconds null.
- Synthetic PlantVid003: style SHORTS, text SHORTS, resume 37, byline
  Ann / UCplantedAnnChannel000001, publishedTimeText Tomorrow,
  Annually. Matches every asserted field including durationSeconds
  null (SHORTS is not clock text and parses to null).

**Continuation tokens.** Located independently in the fixtures: page 1
buries `CONTINUATION_1` inside
`continuationItemRenderer.continuationEndpoint.commandExecutorCommand.commands[].continuationCommand`
(the classic `continuationCommand.token` path is absent), pages 2 and 3
use the classic shape. The generic walk fallback catches both, and the
tests pin CONTINUATION_1/2/3 per page. First page and continuation
pages both yield a token, the acceptance box as written.

**Port verbatim.** `extractEntriesAndContinuationPort` was compared
line by line against upstream `extractEntriesAndContinuation`
(reference/upstream/yt-watch-later-tools.user.js around line 174):
identical, including the fallback token collection and the
`playlistVideoListRenderer.continuations[0].nextContinuationData`
path. The port commit 69aaa6f contains only the port.

**Suites, run by the reviewer.**

```
$ npm run check
all files parse
$ npm test
# tests 175  # pass 175  # fail 0
$ npm run test:browser
8 ok
```

CI on the PR head: required `test` check pass
(run 35969668317).

**Rule semantics for missing data.** durationSeconds null and
channelId null both flow through rules.js as missing data, never
matching a remove rule, in both operator directions; covered by the
three safety tests at the top of the file, which were checked to
assert behaviour (evaluate outcomes), not source text.

**Fixture gap honesty.** capture.json records `unplayable: 0`,
`shortsLike: 0`; the README states why. The unavailable and Short
assertions come from the untouched synthetic fixture, and the gap is
documented in the plan, the PR body and the pre-merge review. No
fixture was fabricated.

**Repo rules.** Author is Kamran Rouhani on all five commits, no AI
markers, no em or en dashes in the changed files or task files, no
comment addressed to a person, task files in their own commits, UMD
wiring matches the other src/core modules (not added to manifest.json,
which is correct: it is a pure function with no page-world need, same
as rules.js), plan frozen and committed before code, synthetic and
captured fixtures unmodified.

## Findings

1. `tasks/0006-playlist-parser/implementation/2026-09-24-playlist-parser-progress.md`
   has entries for steps 1 to 4 and ends with "Next: step 5, self
   review, PR", but there is no step 5 entry, while commit 2ee9056 is
   titled "log steps 2 to 5 in the playlist parser progress". GOAL.md
   hard rule 7 requires an entry at the end of every step and the
   done-list requires "an entry per step". The commit message also
   misdescribes its own content: 2ee9056 adds the step 2, 3 and 4
   entries only (progress log went from step 1 alone to steps 1 to 4).

   Required: a step 5 entry in the progress log, append-only, in the
   established entry shape (what landed, what did not work, real
   command output, the PR round 1 handoff). Preferred fix per the
   repo's rebase preference on unmerged branches: reword 2ee9056 to
   say steps 2 to 4 and land the step 5 entry in its own tasks-only
   commit (or folded into the reworded commit); a plain appended
   entry with 2ee9056 left mislabeled is the acceptable minimum.

No other findings. Correctness, safety semantics, acceptance boxes,
authorship and task structure are all clean; this is the only defect
and it is bookkeeping, not behaviour.

## Verdict

Request changes: one finding, correctable in a single tasks-only
commit plus an optional reword.
