# Round 2 review, 2026-09-24 07:53 (UTC)

- Stage: round 2 independent review, execution lens (rework verified by
  running it, not re-reading it), before merge.
- Branch: `feature/0006-playlist-parser`
- Merge base with main: `b741d69e800991523c5458406d5cb954ab991dfd`
- Head: `c8310fa8e31bd7ae65cd19320a7af21fb2ba0909` (equals the PR head,
  `gh pr view 37 --json headRefOid` at 07:50)
- PR: https://github.com/kamranrouhani/yt-watch-later-cleanup/pull/37
- History note: the round 1 review was written against head `2ee9056`.
  The rework rebased the branch once (content-identical trees, verified
  below), so the rewritten history this review covers is
  `5729959..2a1b5a3` plus the two new round 2 commits:

```
c8310fa note the null and watchedPercent semantics in the Entry contract
1d50ee3 log step 5 in the playlist parser progress
2a1b5a3 add the round 1 review for the playlist parser
4f21b5f log steps 2 to 4 in the playlist parser progress
f14e9d4 add the pre-merge review for the playlist parser
6210b8e restructure the parser to parsePage and the Entry shape
69aaa6f port extractEntriesAndContinuation from upstream verbatim
5729959 add the plan for the playlist parser
```

- Reviewer: metis-review, kanban run 14 on t_ef9412e8.

## Round 1 finding, re-tested

Finding 1 asked for a step 5 progress entry and, preferably, a reword
of the mislabeled commit. Both landed:

- The step 5 entry exists (progress log lines 96 to 141), in the
  established shape: what landed, the PR handoff with its head hash, a
  "Did not work" section that records the original 2ee9056 mistake and
  how it was fixed, real command output (175/175, 8 ok), and a Next
  line. Committed alone in 1d50ee3, tasks only (47 lines added to the
  progress log, nothing else in the commit).
- 4f21b5f reads "log steps 2 to 4 in the playlist parser progress",
  which matches its content. The rewrite is content-identical: after
  the rebase, `git diff 2ee9056 2a1b5a3` shows only the round 1 review
  file, byte for byte identical to the file at 9c184d0 (the review
  commit pushed in round 1). `git diff` between 9c184d0 and 2a1b5a3 for
  that file is empty. The reviewed round 1 record is preserved.
- The step 5 entry itself pins the pre-rewrite head
  `2ee905611d6c81366a3c0bc85df05222c4a82c9f` as "head at review time",
  which is exactly the right way to record a rewrite honestly.

## Operator audit items, verified

1. Entry contract vs docs/ARCHITECTURE.md: c8310fa updates the Data
   model section. The doc now states channelName, channelId and
   unavailableReason are null when YouTube gives nothing, that no field
   is undefined, and why null is the right call (rules.js isMissing,
   line 23, `v === undefined || v === null`, treats both as missing, so
   a null can never decide a removal). Checked in rules.js directly.
2. watchedPercent: the doc states a missing overlay stores 0, not
   null, because 0 is a true reading and the clamp is the safety
   property, and that no rule needs to distinguish 0 watched from not
   told. This matches the parser's tests and rules.js behaviour
   (numeric comparisons always receive a number).
3. Fixture gap: the PR body says plainly that the capture has no
   unavailable item and no Short (capture.json `unplayable: 0`,
   `shortsLike: 0`), that both are asserted from the untouched
   synthetic fixture, and that nothing was fabricated. Confirmed
   against the round 1 review's own re-derivation of those synthetic
   assertions and `git log` showing no fixture files touched.
4. The port/parsePage pair is genuinely tested against each other:
   `test/playlist-parser.test.js:320` runs both over all three captured
   pages and asserts equal token, equal entry count and equal
   setVideoId set per page; line 332 pins the port's upstream field
   names. Either test reddens if parsePage drifts from the port. The
   file's comment (playlistParser.js:4) states the purpose without
   addressing a person.

## Suites, run by the reviewer at head c8310fa

```
$ npm run check
all files parse
$ npm test
# tests 175  # pass 175  # fail 0
$ npm run test:browser
8 ok
```

CI: `gh pr checks 37` shows the required `test` check pass (39s, run
35971661998).

## Repo rules

Author is Kamran Rouhani on all eight commits (verified via
`git log --format='%an <%ae>'`), no AI markers, no em or en dashes in
the changed files, no comment addressed to a person, task and doc
commits stay separate from code commits, worktree clean and equal to
the pushed PR head.

## Verdict

Approved. The single round 1 finding landed exactly as requested and
all four audit items are resolved with evidence. No new findings. The
previously verified correctness work (field assertions, port verbatim,
safety semantics, acceptance boxes) is unchanged by the rework: the
code files in 2a1b5a3..c8310fa are untouched, both new commits touch
only tasks and docs.
