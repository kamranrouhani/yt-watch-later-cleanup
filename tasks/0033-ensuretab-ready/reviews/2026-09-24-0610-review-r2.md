## Review round 2: #33 ensureTab waits for the tab to answer

- **Stage:** review lane, round 2 (execution lens), PR #34
- **Branch:** fix/0033-ensuretab-ready
- **Merge base:** 6acc848f37a9e90a8bd4e9538c726db5db593d9f
- **HEAD:** 36b2c4c865654c51615cd805d232811216eb8265 (equals the PR head)
- **Covered:** 15e460e..36b2c4c, plus a full rerun of the branch
  ```
  36b2c4c rename and split the ensureTab progress log per review
  3016aec reject ensureTab when the tab closes mid readiness wait
  ```

## Verdict

Approved. All three round 1 findings landed and every acceptance box holds
when run.

## Round 1 findings

1. **Closed tab counted as ready: fixed.** `probeOnce` now rejects the outer
   promise when the pending entry is rejected with `TabGoneError`, and still
   treats any other error reply from page.js as an answer. The new unit test
   closes tab 5 while it is `listening` and expects `TabGoneError` with
   `connectedTabId` null. Red first, rerun here: HEAD's test file against
   15e460e's `src/core/tabBridge.js` gives `not ok 13` and `# pass 14 # fail 1`.
   Against HEAD, `# pass 15 # fail 0`. The progress log records the same
   failure (`Missing expected rejection (TabGoneError)`).
2. **Progress log names: fixed.** `implementation/` now holds
   `2026-09-24-ensuretab-ready-progress.md` and
   `2026-09-24-1-ensuretab-ready-e2e-probe-progress.md`, in the tasks-only
   commit 36b2c4c. `git diff 5d09448 HEAD -- tasks/0033-ensuretab-ready/plans/2026-09-24-ensuretab-ready.md`
   is empty.
3. **Mock-narrating comment: removed** from the fake `sendMessage` in
   `test/tabbridge.test.js`. The spec comment about `chrome.tabs.create`
   bypassing `context.route` stays, as agreed.

## Runs

- `npm run check`: `all files parse`.
- `npm test`, 5 consecutive runs: `# pass 148 # fail 0` every time. The
  remover.test.js timing flake from round 1 did not show this time.
- `npm run test:browser`, 3 runs: 8 ok lines each, including "closing the tab
  then calling ensureTab again opens a fresh one and ping succeeds".
- CI: required `test` check green on 36b2c4c.
- Authorship: all commits Kamran Rouhani <kamran.rouhani@outlook.com>, no
  trailers or tool markers. No em or en dashes in changed files.

## Residual, not blocking

- If the tab closes between two probes rather than during one, no probe is
  pending, `sendMessage` fails, and the loop keeps polling until the deadline,
  so ensureTab ends with `TimeoutError` instead of `TabGoneError`. The outcome
  is still a rejection with `connectedTabId` null, only the error class and
  the wait differ. Worth a follow-up if the dashboard ever shows those two
  errors differently.
- The `test/remover.test.js:175` timing flake from round 1 still needs its
  own issue.
