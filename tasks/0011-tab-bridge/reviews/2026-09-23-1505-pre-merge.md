# Review: pre-merge

- **When:** 2026-09-23 15:05
- **Stage:** after step 4 of 2026-09-23-tab-bridge.md, before the PR
- **Scope:** commits
- **Branch:** feature/0011-tab-bridge
- **Base:** main @ 70d2315
- **Head:** b329b2b
- **Commits reviewed:**
  - b329b2b log the tab bridge work and its manual checklist
  - 8e10266 bridge the dashboard to a youtube tab through both worlds
  - e9f0543 add the plan for the tab bridge
- **Reviewer:** self

## Acceptance

| Box | Met | Evidence |
|---|---|---|
| unit: a message with a wrong or missing nonce is ignored | yes | page world tests: undefined, '', 'wrong', null nonce all ignored, plus the before-hello case. Seen red when the guard was dropped |
| unit: the relay forwards a request and its response unchanged | yes | request relayed with kind, id, payload and a 32 hex nonce; response relayed field for field |
| manual, recorded in the PR: signed-in ping returns the client version | checklist | `implementation/manual-check.md` step 3, expected observation stated; the build host cannot sign in |
| closing the tab mid request produces a visible error, not a hang | yes | unit TabGoneError both via onRemoved and via sendMessage failure; browser spec closes the routed tab mid delayed request and gets TabGoneError |

Scope from the issue: MAIN world `content/page.js` running the core client
from `ytcfg` and cookies, isolated `content/bridge.js` relaying, the
per-session nonce, dashboard finds or opens the tab, double injection a
no-op. All present. The manifest carries both entries, and the wiring test
pins the MAIN world js list exactly.

## Checks

- **Attribution:** none. Author is Kamran Rouhani.
- **Dashes:** none in changed files.
- **Comments:** none in shipped code.
- **Secrets:** fake key, fake cookie, fake visitor data in tests only.
- **Task files separate:** e9f0543 and b329b2b touch only `tasks/`.
- **No-network guard:** green with `content/` scanned as a shipped dir.
- **CSP:** the dashboard loads `tabBridge.js` via a script tag, not
  injection, so the extension's `script-src 'self'` holds.

## Findings

1. This host cannot load the real youtube.com: it redirects to
   consent.youtube.com from this egress IP, which matches no content
   script pattern. The routed fake page is the only injection proof
   possible here; the real signed-in ping is manual checklist step 3.
2. The isolated world's globals are invisible from the page world, so
   `__wlBridgeInstalled` cannot be asserted from Playwright's page context.
   The handshake observable is `page.js` setting `__wlReady` after taking
   the nonce, which is the page-side proof that hello arrived with a valid
   nonce.
3. My bite tooling corrupted `content/page.js` once (backup taken after
   the edit, restore from a stale backup). Caught by the suite, repaired,
   bites redone in the right order. Recorded in the progress log.
4. Nothing blocks the merge.
